name: Let's Build and Deploy Go with Docker


on:
 workflow_dispatch:
    inputs:
       
      dockerimage-name:
          description: 'Docker Image Name '
          required: true
          type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
<<<<<<< HEAD
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/${{dockerimage-name}}:latest
=======
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/go-docker-image:latest
>>>>>>> 5153dde63b283ea1dd4bf6affdbaa58303fec968

     
